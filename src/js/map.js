       function myMap() {
          var mapProp = {
            center: new google.maps.LatLng(49.841321, 24.031300),
            zoom: 10,
          };
          var map = new google.maps.Map(document.getElementById("googleMap"), mapProp);
        }
